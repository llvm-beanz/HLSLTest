name: macos-nightly

permissions:
  contents: read
  checks: write

on:
  schedule:
    - cron: '0 */6 * * *' # run at 2 AM UTC

jobs:
  windows-latest-build:
    uses: ./.github/workflows/test-all.yaml
    with:
      OS: macOS
